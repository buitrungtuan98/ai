# System Map

Living dependency map — **one row per module/file**. This is the source of truth for "what exists,
what it does, and what depends on what". Updating this table is part of the Definition of Done: add
a row when you add a file, bump `Last updated` when you change one.

Status tokens: `DONE` · `WIP` · `TODO` · `BLOCKED`.

## Application code

| Path | Layer | Responsibility | Inputs | Outputs | Depends on | Status | Last updated |
|------|-------|----------------|--------|---------|------------|--------|--------------|
| `core/config.py` | config | The only place env/`.env` is read; `settings` singleton, fail-fast validation; `IMAGE_TIMEOUT_SECONDS`/`IMAGE_WAIT_BUDGET_SECONDS` (slow-vendor healing, ADR-069); `GEMINI_IMAGE_MODEL` (Studio-Mode image PROVIDER chain, managed separately from the text `GEMINI_MODEL` — a `pollinations:flux` entry uses Pollinations) + optional `POLLINATIONS_TOKEN` + `PUBLIC_BASE_URL` (public host for kontext to fetch a character reference, ADR-055); `AI_CALL_TIMEOUT_SECONDS` — per-attempt cap on every Gemini text/vision call (R22) | `.env` | `Settings` | pydantic-settings | DONE | 2026-08-04 |
| `core/compilation.py` | render | Best-of compilations (ADR-082) — the long-form money format built from work already done: published masters are RETAINED into a per-campaign library at publish (moved, capped at 24 newest, fail-open to plain deletion), `compilable_episodes` ranks them best-retention-first, `compilation_metadata` writes the title/chapters/description deterministically in the campaign language (chapter lines double as YouTube chapters), sentinel episode numbers 9001+ keep unique(campaign,episode) intact and never advance the campaign; honest bounds in the docstring (post-ship masters only, review-always); owns `is_ordinary`/`ordinary_episodes` — THE population filter keeping sentinel episodes out of every learning statistic (ADR-085) | worker | paths + metadata | video_worker, council | DONE | 2026-08-04 |
| `core/council.py` | ai | The strategy council (ADR-081) — map-reduce with one non-negotiable boundary: code computes every number (`evidence_pack`: classification, flop counts + consecutive run, publish-hour → avg first-day-views table, A/B retention, QC scores, monetization progress, the council's own recent decisions), ONE structured Gemini call per channel per day interprets it (temperature 0.3, CLOSED action menu extend/wind_down/successor/tune/slot_change/hold), and deterministic rails validate each decision — params in bounds (extend ≤ +50%, slot must exist, 1 applied slot-change/week), campaign live, and every number ≥10 in the model's prose must literally exist in the pack (the anti-hallucination rail); cached on the evidence hash (own decisions excluded, or every verdict would invalidate its own cache); refused decisions are logged as `AutopilotAction(kind="refused")` with wanted/refused-because/confidence (glass box — ADR-084) and dropped — the deterministic autopilot underneath never depends on this layer; the action menu includes `compile` (rails: ≥10 measured episodes, top_n 4-20), applied by creating + queueing a build that always parks for review (ADR-082); slot_change targets must be MEASURED, not merely well-formed: the destination (±1h) needs real first-day numbers that beat the hour being abandoned; the pack carries per-campaign `operations` health (failures/streak/runway/no-verdict QC) + `operator_recently_dismissed`, and `_already_proposed` honours the shared 30-day dismissal cooldown (ADR-086) | scheduler | AutopilotAction proposals | ai_engine, autopilot, flop, monetize | DONE | 2026-08-04 |
| `core/monetize.py` | learn | Monetization scoreboard (ADR-080): distance to being PAID in each platform's own currency — YPP long route (1k subs + 4,000 watch-hours/365d) and Shorts route (10M views/90d, labeled an approximation because the free API doesn't split Shorts out), FB in-stream (10k followers + 600k watched-minutes/60d, summed from OUR episodes — a labeled lower bound); no data → no bar, never a guess; `crossed_milestones` powers the once-per-level 80%/100% announcements | learn | progress dicts | analytics, scheduler | DONE | 2026-08-03 |
| `core/flop.py` | learn | Early-flop detection + autopsy (ADR-079): `views_24h` is stamped once on the first hourly refresh past 24h, a flop = <30% of the campaign's OWN median first-day views (judged only past 5 measured episodes — silence beats a guess), `record_flop` writes a prompt-ready autopsy note (views vs typical, variant, local publish hour, premise) into `learning_json.flop_notes` which the script generator already consumes as avoid-notes, and `late_autopsy_hook_note` upgrades the note when the retention curve blames scene 1 — the loop closes with no human in it; the median counts ORDINARY episodes only (ADR-085) | learn | verdicts | analytics_service | DONE | 2026-08-04 |
| `core/slop_gate.py` | ai | Pre-render script quality gate (ADR-079, 0 AI calls): word-3-gram similarity vs the campaign's recent episodes catches "the model wrote the same episode again" (block ≥50%, warn ≥35%), duplicate titles block (a spam signal to both platforms), a cliché blacklist (VN+EN defaults merged with the operator's Settings additions) and a rambling-hook check warn; verdicts ok/warn/block, `script_fingerprint` is the narration+title record a finished episode leaves in `Task.render_json` for future gates. ADR-089: `strip_phrases`/`without_phrases` remove the campaign's MANDATED catchphrases from both sides before anything is compared — forced branding is not the self-repetition this gate exists to catch (measured here: two different scripts score 0.00, the same pair carrying one catchphrase pair scores 0.24 at ~65 words and 0.49 at quote length, a hair under the 0.50 block), and a mandated phrase can no longer trip the operator's own blacklist; a duplicate-title issue now QUOTES the title it duplicates (the regenerating model only ever sees synopses, so "duplicates episode 12" named something it could not look up) | worker | GateReport | — | 2026-08-05 | 2026-08-03 |
| `core/creative_brief.py` | core | The campaign's creative brief as DATA (ADR-089): `CONFIG_KEYS` names the settings that reach the scriptwriter or the gate judging its output (everything else — voice, music, captions, footage, scheduling — shapes the video around an accepted script and must NOT count as a new brief); `key_digests` hashes each one (plus the campaign topic/total and the channel's clamped judge threshold and the user's slop blacklist) so a brief VERSION can be recorded on a Task and in the audit log without copying the operator's persona/system-prompt words anywhere; `fingerprint` (same brief?), `changed`/`describe` (which fields moved?), `edited_since_gate_failure` (has the brief moved since this episode's script was refused?) and `active_catchphrases` — the ONE definition of "which catchphrases are mandated", shared by generation, the similarity gate and the AI judge, whose disagreement made a gate no regeneration could pass | campaign/channel/user rows | digests, changed keys | stdlib | DONE | 2026-08-05 |
| `core/failure.py` | core | ONE classification of render failures (ADR-068/069): the pattern table `PATTERNS` (with a Facebook-token class of its own, ahead of the API-key one, because their fixes live on different pages — /channels vs /credentials, ADR-072) (match words → cause, fix, where-to-act, transient?; ORDER is load-bearing — the stalled class sits ahead of the network class because the watchdog's and reaper's own wording contains both "stalled/worker" and "timeout", ADR-070), `diagnose` (episode page / dashboard triage / alert bell all say the same thing) and `is_transient` (the autopilot's retry gate — a retry can't mint a key, refill a quota or unblock deterministic content; unknown errors count as transient, the retry cap bounds the optimism). Deliberately a fixed table, not an AI call — it must work when the AI is what broke, and it returns None over guessing; the quality-gate class (non-transient — a retry regenerates against the same evidence, so it needs a human, not the autopilot's cap); the thin-library compile class (non-transient — it heals by publishing more episodes, not by retrying the concat); `quota_reset_since` — the one non-transient class that heals by waiting becomes retryable after the Pacific midnight reset (ADR-085); fix copy is reader-anchored — it may only point at things visible from every surface that renders it (the triage row shows no raw error, so no fix may say "see the raw error"; episode-page-only buttons are named WITH "open the episode", R21); R22: classification runs on the exception SUMMARY (`_classification_text`) — never a stored traceback's frame paths (the bare word 'worker' used to match `workers/video_worker.py` inside every trace and shadow all rows below); numeric words match digit-bounded; new rows: rejected-in-review (first, so free text can't reclassify), YouTube `invalid_grant`, upload-interrupted, model-not-found, buffer-aged-out; quota row dropped 'exceeded'/'rate limit' (network-shaped, retry next tick not next Pacific midnight); `is_infrastructure`/`is_reject`/`is_human_reject` feed the circuit breaker + retry policy. ADR-089: the quality-gate class SPLIT in two, because they need different advice and only one names a movable threshold — `script judge scored it` → "the judge scored two drafts below this channel's bar" → /channels (naming the "Reject at QC ≤" dial), the deterministic block → "repeated an earlier episode too closely" → /campaigns; both dropped the "trim the blacklist" advice, which could never work (the blacklist only ever warns). New Facebook class for a token that is ALIVE but may not post (Graph #200 / `pages_manage_posts`): it carries none of the OAuth words, so it fell through to the transient default and was retried by the autopilot AND re-published at every posting slot, failing identically each time | error text | cause/fix dict, retryable? | stdlib | 2026-08-05 | 2026-08-04 |
| `core/timezones.py` | config | Curated IANA timezone catalog for the campaign/channel picker (`tz_choices` grouped + live DST-correct UTC offsets, `is_valid`, `offset_label`); single source replacing free-text zone entry | — | grouped choices, validation | zoneinfo | DONE | 2026-07-24 |
| `core/host.py` | ops | Host vitals from the kernel — CPU **load average** per core (`os.getloadavg` + `sched_getaffinity`, so a container's cpuset is respected) and memory used as Total−**Available** (page cache is reclaimable; counting it would read as 95% on a healthy box). Stdlib only — no psutil dependency for two numbers Linux publishes (ADR-062); every reader fails soft to None so a non-Linux dev box renders "—". Disk stays in `main._system_health` (one definition) | — | vitals dict | stdlib | DONE | 2026-07-28 |
| `core/security.py` | security | Fernet/MultiFernet encrypt/decrypt util (key rotation, None-safe) | `FERNET_KEY` | ciphertext/plaintext | cryptography, config | DONE | 2026-07-17 |
| `database/types.py` | data | `EncryptedString` TypeDecorator + enums (incl. SCHEDULED, AWAITING_REVIEW, awaiting_review/rejected, and `TaskStatus.CANCELLED` — an operator's own decision, deliberately NOT a failure: excluded from the failure KPI and the alert feed, skipped by autopilot's auto-retry, and treated as a finished outcome by hydration — ADR-064) | column values | encrypted columns | security | DONE | 2026-07-28 |
| `database/models.py` | data | ORM: Users→Channels→Campaigns→{Tasks,BufferPool}; `Channel.autopilot_json` (per-channel autopilot mode/cadence/review thresholds) + `Channel.profile_json` (per-channel persona/localization: audience/language/timezone/voice/style/vision); user `gemini_model` chain + `gemini_image_model` (Studio-Mode image provider chain, managed separately) + `pollinations_token` (optional, encrypted — Studio image fallback) + `settings_json` (per-user preferences: new-campaign defaults + AI daily budget); `Channel.characters_json` (Studio-Mode cast: consistent characters an AI-drawn episode picks from); task timing/retry/published-url/synopsis/ab_variant columns + `Task.render_json` (scene map + duration + render_seconds, for retention attribution + honest render-time display); `BufferPoolItem.publish_at` (optional per-episode publish-time override set from the Operations page — ADR-059); `ChannelSnapshot` (one row per channel per LOCAL day: subscribers/views/videos straight from the platform — the only record of channel-level growth over time, since platform totals are never the sum of the episodes we made; unique(channel, day) makes collection idempotent — ADR-063); `ChannelClipUsage` (per-channel Pexels-clip history for cross-episode footage dedupe) + `AutopilotAction` (the autopilot proposals inbox / audit log / idempotency guard — evidence + params + status) — both new tables, auto-created by `create_all`; `Task.auto_retry_count`/`auto_reject_count` are the AUTOPILOT's own budgets, kept apart from the operator-facing `retry_count` that every path increments, and `Channel.analytics_error` records why a channel's stats cannot be read (ADR-076); `ChannelSnapshot.watch_minutes_365d`/`views_90d` — the YPP windows (ADR-080); `BufferPoolItem.ready_at` — when the item became publishable; expiry ages from it (R22) | — | ORM classes | SQLAlchemy, types | DONE | 2026-08-04 |
| `database/db_session.py` | data | Engine + WAL PRAGMA listener, `SessionLocal`, `get_db`, `init_db` (+ additive column upgrades incl. `ab_variant`, `tasks.render_json`, `users.gemini_model`, `users.gemini_image_model`, `users.pollinations_token`, `users.settings_json`, `channels.autopilot_json`, `channels.profile_json`, `channels.characters_json`, `buffer_pool.publish_at`); additive upgrades gained `tasks.auto_retry_count`/`auto_reject_count` + `channels.analytics_error` (ADR-076); `buffer_pool.ready_at` column upgrade (R22) | `DATABASE_URL` | sessions | SQLAlchemy, config | DONE | 2026-08-04 |
| `auth/dependencies.py` | auth | `get_current_user` (Bearer/session/solo), ownership guards incl. buffer items (404), `get_or_create_user` | request | `User` | firebase, models, config | DONE | 2026-07-18 |
| `auth/firebase.py` | auth | Lazy Firebase Admin init, `verify_id_token`, `sign_in_with_google_id_token` (REST signInWithIdp) | tokens | claims / firebase sign-in | firebase-admin, requests, config | DONE | 2026-07-18 |
| `core/ai_engine.py` | ai | Gemini wrapper (fallback chain, quota fail-fast, call metering); persona+playbook prompt composer; episode memory (synopsis REQUIRED in schema); critic loop (incl. grammar/diacritics gate); playbook distiller (also consumes retention drop-off notes — same call); vision judges (single + batched footage, final QC — audio-aware when a track is attached); campaign designer (voices derived from `tts.VOICE_CHOICES` — narrowed to `tts.QUOTE_VOICES` and to the target language for a `content_style="quote"` campaign, with the prompt asking for a soft confiding read and a lofi/piano music mood, ADR-071; channel-profile-aware — `_profile_line` localizes the design to the channel's audience/vision/style + local prime-time posting; `video_format`-aware — the operator's short/long choice is designed FOR in the prompt, forced onto the result, and its durations clamped to the format range so a Long proposal is genuinely long-form); duration targeting (word budget + length fix); script depth — optional `deep` mode runs a research `EpisodeBrief` pass (facts + hook→build→payoff→cliffhanger arc) that conditions the script call; per-language AI-cliché blacklist (`AI_CLICHES`/`find_cliches`) that steers the script + critic prompts and gates one deterministic post-draft rewrite; format-aware prompt (short vertical vs long-form 16:9 with more scenes + part-numbered titles); weekly autopilot strategist (`suggest_channel_tune` — one call reads a channel scorecard, returns a bounded creative tweak: caption theme / music mood / TTS rate); live model listing + curated free-tier limit catalog (`GEMINI_MODEL_CATALOG`); quote content style (`build_quote_prompt` — a poem, one line per scene, + optional `cover_word`; ADR-056); Studio-Mode image generation (`generate_image` — a PROVIDER chain, ADR-053: Gemini image model with reference-image conditioning for character/temporal consistency OR Pollinations via `_call_pollinations` (current `gen.pollinations.ai/image` endpoint — the legacy `/prompt` host 500s for kontext; free, keyless; `flux` text-to-image with a deterministic per-prompt seed, or image-editing models `kontext`/nanobanana/gptimage/seedream that take a `reference_url` as `image=` to honour an uploaded character, degrading to text-only `flux` when no reference URL is available, auth via the `Authorization: Bearer` header per the API docs (a `token` query param is ignored → anonymous tier; header also keeps the secret out of URLs), deterministic 401/402/403 gates raised with actionable messages, scrubbing the token from any error (never logs the secret), and a last-resort flux safety net when the whole chain fails so Studio still renders — ADR-055); any entry may lead, any failure falls to the next EXCEPT a content block, which is terminal; budget metering, quota/404 fail-fast; slow-vendor healing (ADR-069): per-attempt timeout LADDER — base ×2 per retry, capped 600s, throttle-friendly longer backoff — inside a per-episode `deadline` (`_attempt_timeout`; a spent budget fails fast with a message that classifies as transient), a `seed_salt` that breaks the free provider's prompt-derived determinism on demand — Auto-QC's re-render was rebuilding the rejected video pixel-for-pixel and re-judging it (ADR-070); and atomic image writes (`_write_image_atomic`, tmp+rename) so the checkpoint reuse guard can trust any non-empty still); schemas; `judge_script`/`ScriptVerdict` — the pre-render script judge (ADR-079 C2), scored on the same /10 scale as vision QC; R22: every text/vision call passes `request_options={'timeout': AI_CALL_TIMEOUT_SECONDS}` so a brownout provider fails fast instead of wedging the worker for the SDK's ~600s default. ADR-089: `judge_script(required_phrases=…)` tells the judge which lines the writer was ORDERED to include — told nothing, it read a mandated opening as a weak hook and a mandated closing as filler, scored the draft down for both, and no regeneration was allowed to fix either | topic, cfg, learning, frames, audio | script + metadata + critiques + playbooks + verdicts + proposals + briefs + model lists + drawn images | google-generativeai, pydantic, Pillow, config, usage, tts | 2026-08-05 | 2026-08-04 |
| `core/autopilot.py` | ai | Data-driven channel autopilot decision engine (pure/read-only; 0 AI calls). Phase I: `classify_campaigns` labels each campaign (winner/healthy/underperforming/too_early) vs its channel's own retention baseline (`channel_baseline`). Phase II config + decisions: `ap_mode`/`ap_interval_seconds`/`review_thresholds` read `Channel.autopilot_json`; `review_decision` turns a render's stored QC verdict into approve/reject/escalate (low score or failed QC → reject; high → approve; middle/verdict-less → escalate). Phase III proposals: `propose_actions` files deterministic, reversible strategy suggestions (extend a winner near its cap / plan a successor for a healthy one / wind down a laggard with a long low streak). K3 audience verification: `audience_summary` aggregates measured episodes' top-viewer country and flags off-target channels (dominant country vs the profile language's expected set) → an advisory drift proposal. The scheduler executes these.; `MEASURES_RETENTION`/`measurable(channel)` name the platform limit that used to be silent — only YouTube reports “% viewed”, so every verdict here is unavailable on Facebook and the UI says so instead of grading every campaign “healthy” forever (ADR-076); the flop breaker: 3 straight first-day flops propose a wind-down days before retention could (ADR-079), guarded by the same ≥5-measured floor as the flop verdict itself. ADR-089: `SCRIPT_JUDGE_REJECT_MAX`/`script_judge_reject_max` give the pre-render SCRIPT judge its own ceiling — it used to reuse the channel's video-QC reject threshold verbatim (ADR-079 C2), so a channel tightened to "Reject at QC ≤ 7" demanded 8/10 from every script and could not render at all (a good draft scores 7 with two style notes); a looser channel keeps its own value, a stricter one is clamped. `log_action` moved here from the scheduler: the web layer records campaign edits to the same audit trail, and two writers would be two definitions | campaigns, tasks/stats, qc verdicts | classifications, review verdicts, proposals, config | models, SQLAlchemy | 2026-08-05 | 2026-08-04 |
| `core/safety_filter.py` | ai | Profanity/brand-safety term filter; Pexels license check; variation/ToS policy gate | script text, flags | filtered text, gate result | stdlib | DONE | 2026-07-17 |
| `core/vibe.py` | ai | Vibe Engine (ADR-056) — per-episode creative roll for the "quote" content style: seed-deterministic pick of mood / one-off-character-or-scenery / setting / music-mood / voice-pace jitter from curated pools, so every quote video is unique while the channel's art style stays constant (the style is NOT rolled here) | seed, character_ratio | vibe dict | stdlib | DONE | 2026-07-26 |
| `core/retention.py` | ai | Pure retention-curve analysis (0 AI/IO): `scene_map` (per-scene absolute spans + labels), `drop_points` (attributes the steepest audience drop-offs to the scene playing there), `summarize_drop` (one human line for the Episode view + playbook distiller) | curve + scene map | drop findings, summary | stdlib | DONE | 2026-07-24 |
| `core/ffmpeg_runner.py` | render | DRY subprocess runner: `nice -n 19`, `-threads 4`, `-progress` → progress %; `extract_frame` + `extract_audio` (ADTS stream copy for audio QC) | ffmpeg args | files, progress callbacks | ffmpeg (system) | DONE | 2026-07-21 |
| `core/tts.py` | render | edge-tts synthesis; returns mp3 path + word-boundary timings; retries transient endpoint failures; paced synthesis (`synthesize_paced`) renders each sentence separately and stitches them with deterministic breath gaps (longer after `?`/`…`), returning ONE merged timing list with absolute offsets so captions still align; `VOICE_CHOICES` — THE per-language voice catalog (form dropdown + AI designer both derive from it); soft delivery (ADR-071) — `QUOTE_VOICES` (the curated intimate-read voices per language, marked 🌙 in the picker, auto-picked by Quote and the only pool the designer may propose for a quote campaign), `_delivery_params` (rate −12% + a PER-VOICE pitch drop — an already-deep voice drops far less or it sounds unwell) and `SOFT_VOICE_FILTER`/`build_soft_voice_args` (highpass → lowpass → compressor → tiny room, all duration-preserving so the word timings still drive the captions, applied ONCE to the assembled narration by `apply_soft_voice`, never per sentence). Not a real whisper: edge-tts has no whispering style and Azure's paid one has no vi/es voice at all | narration, voice, rate, delivery | mp3 + timings | edge-tts, media, ffmpeg_runner | DONE | 2026-07-29 |
| `core/media.py` | render | ffprobe helpers (duration/codec) + `probe_audio_stats` (volumedetect mean/max dB) + `max_black_span`/`max_silence_span` (blackdetect/silencedetect parsing for deterministic QC); audio duration = ground truth | media path | duration/codec/volume/black/silence info | ffprobe/ffmpeg (system) | DONE | 2026-07-23 |
| `core/pexels.py` | render | Pexels stock-footage search + download (orientation-selectable: portrait for shorts, landscape for long-form; `_best_file` picks the orientation-matched SMALLEST rendition clearing the 1080 short-side floor — sharp without a wasteful 4K download; sub-floor clips sort last) | query, api key | clip metadata, files | requests | DONE | 2026-07-24 |
| `core/studio.py` | render | Studio Mode domain logic (ADR-052) — the 2nd visual source: `scene_cache_key` (prompt-hash still names — checkpoint resume can only ever reuse the SAME drawing, ADR-069); `pick_character` (random-but-seed-deterministic per episode so a re-render reuses the cached sheet), art-style resolution (campaign override > character's own > house base), `character_sheet_prompt`/`scene_prompt` (identity + style + the operator's beat direction: dynamic pose, action lines, motion blur, no in-image text), `character_sheet` (draw-once + cache) + `scene_visual` (also supports NO character — quote content style draws pure atmosphere in the fixed art style, ADR-056; conditioned on the sheet + previous frame for the Gemini leg, and a public `reference_url` for the Pollinations kontext leg — ADR-055); `gen_image` seam is injectable for tests | cast, scene subject, refs | image prompts + drawn PNGs | ai_engine | DONE | 2026-07-28 |
| `core/captions.py` | render | Word/line ASS subtitles with caption themes (classic/highlight/boxed/neon, accent colour, pop animation); PIL wrapping; `width`/`height` params so PlayRes matches any output geometry (vertical or 16:9); a centered faded **quote** caption style + a lower-centre **signature** mark (ADR-056); optional top **billboard headline** (ADR-054) — a bold UPPERCASE two-tone title (white + accent, `split_two_tone`) burned above the captions for the whole clip; the billboard headline is a HOOK FLASH (ADR-078): `teaser()` cuts the drawn title at a word boundary (~56 chars — the published title is untouched), the two-tone block is fitted into ≤`HEADLINE_MAX_ROWS` rows by shrinking from ~4% of frame height (the old 5.2% with no cap let a 13-word hook eat half the frame), and the event spans only `HEADLINE_FLASH_S` (3s) with a fade-out instead of the whole clip | timings, theme, dims, headline | `.ass` file | Pillow | DONE | 2026-08-03 |
| `core/thumbnail.py` | render | PIL cover + wrapped title + optional logo; `width`/`height` params (9:16 or 16:9); when given the duration, picks the sharpest/most-colourful of several sampled frames (edge + colour score) instead of one blind mid-video grab; optional **poster** style (ADR-054) — the same two-tone billboard title at the TOP so the thumbnail matches the in-video headline; a **scribble cover** (one word centered over the illustration — ADR-056); `_select_frame(min_at_s=…)` keeps every sample (and both fallbacks) after the hook-flash window — the poster used to be drawn over a frame that already carried the burned title, and `_frame_score` rewards edge density so text-heavy frames were actively preferred (the doubled-text thumbnail an operator reported, ADR-078); the poster title uses the same `teaser()` cut as the flash | frame, title, dims | `.jpg` | Pillow, ffmpeg_runner | DONE | 2026-08-03 |
| `core/cleanup.py` | render | `RenderWorkspace` context manager (success cleans; a FAILED render keeps its workspace as a resume checkpoint — ADR-069) + `sweep_orphans` (nothing >60 min; the scheduler passes skip sets: in-flight renders + recently-failed tasks whose checkpoint a retry would resume from) | job id | removed temp dirs | stdlib | DONE | 2026-07-28 |
| `core/video_factory.py` | render | Orchestration (`COLOR_GRADE_CHOICES` — the ONE selectable-grade catalog the form and the create-time whitelist both read, asserted against `COLOR_GRADES`, which is how the ready-and-correct `vintage` grade stayed invisible for a release; `voice_delivery` forwarded to the TTS so a quote campaign is read softly — ADR-071; Studio scenes checkpoint-resume: stills named by `scene_cache_key`, reused when present+non-empty, all image fetches share one `IMAGE_WAIT_BUDGET_SECONDS` deadline and the Settings-page `image_timeout_s` — ADR-069): 3-phase: prep all scenes (paced TTS + deterministic `voice_check` silence/truncation gate + footage, reordered by `prefer_unused` for cross-episode AND in-episode variety — a growing seen-set steers later scenes off clips earlier scenes consumed)→batched vision vetting→render: `plan_shots` slices each scene into 2–4s shots cut on word boundaries (multi-shot cut rhythm, no clip sits a whole scene), motion effect seeded per episode, grade + captions baked in one pass with lanczos scaling + CRF 21; long-form fades its last scene's tail while shorts stay abrupt for loop rewatches→stitch(copy video + `+faststart` for instant preview; audio mixes music DUCKED under narration via `sidechaincompress`, then −14 LUFS)→thumbnail; `RenderProfile` drives geometry (short=vertical 1080×1920 · long=16:9 1920×1080 with landscape footage + chapter timestamps in the description); returns used Pexels clip ids for dedupe; metadata gets title prefix + affiliate footer + chosen A/B variant; Studio Mode (ADR-052, `visual_source=studio`) swaps the footage source for AI-drawn keyframes — one `studio.scene_visual` per scene (character sheet cached in a stable per-channel dir for cross-episode consistency), each looped into a clip by `still_to_clip` and fed through the SAME motion/caption/stitch stages (fails clearly if the channel has no cast or no image key — never silently falls back to stock; an operator-uploaded character `ref_image` is used directly as the sheet — W4); billboard title (ADR-054) burns the hook title + poster thumbnail; quote content style (ADR-056) renders the Studio path character-less with centered quote captions + signature + scribble-cover thumbnail + `vintage` grade; `title_overlay` is tri-state via `_title_mode` (off | thumb | flash; legacy bool/"on" → flash — strings are truthy, so no raw branching): flash passes the headline to SCENE 0's ASS only (every scene's ASS starts at its own t=0, which is how the old billboard sat over the whole video), thumb keeps the video clean, both draw the poster thumbnail with frames sampled past the flash window (ADR-078) | script, episode, cfg, format | master.mp4 + thumb + metadata + used_clip_ids | all core/*, config | DONE | 2026-08-03 |
| `core/qc.py` | ai | Auto-QC gate: footage vetters (single + episode-BATCH, ≤2 vision calls/episode) + final-video QC runner (frames + audio track in ONE call: captions/visuals AND voice clarity/language/music balance) + `run_deterministic_qc` (free black-frame/silence checks, fail-closed on catastrophic breakage, per-detector fail-open); three honest verdict states — passed / failed / **unavailable** (`QCResult.unavailable` + classified `unavailable_reason`: daily quota, rate limit, model missing, unreachable; an errored judge is never a green pass — ADR-084); deterministic guards even when vision is down | clip/master path, Gemini key | accept/reject, QCResult | ai_engine, ffmpeg_runner, media | DONE | 2026-08-04 |
| `core/usage.py` | ai | Daily AI-call meter (Redis, keyed to Google's Pacific quota day; fail-silent) — powers the dashboard quota chip + heartbeat; `reserve_reached(user)` — THE 80% budget-reserve guard every optional AI consumer shares (user setting else env fallback, ADR-085) | call events | calls-today count | task_queue (lazy) | DONE | 2026-08-04 |
| `workers/task_queue.py` | queue | Single `renders` queue, render lock, Redis progress (each write also change-stamps `task:progress-ts`, so "progress stopped moving" is measurable — ADR-057), `enqueue_render`/`enqueue_publish`, `worker_alive()`, `worker_healthy()` (registered AND not wedged) + `stalled_render()`/`stall_limit_seconds()`, `clear_all_progress()` (boot reset), operator restart flag (`request_worker_restart`/`restart_requested`/`clear_restart_request`); `enqueue_compile` — same queue, one ffmpeg at a time is the law of this box (ADR-082); `enqueue_requalify` (re-judge a parked buffer item in place — ADR-084); R22: `probe_conn` (socket-timeout-bounded second connection) for the watchdog's reads so a wedged-not-crashed Redis cannot hang the recovery thread; `queued_publish_buffer_ids` answers 'is an upload already on its way?' for the expiry sweep + reconciler | `REDIS_URL` | `Queue`, helpers | redis, rq, config | DONE | 2026-08-04 |
| `workers/video_worker.py` | worker | `published_url_for` is format-aware on BOTH platforms (a 15-minute YouTube video is not a Short), `_publish_buffer` carries the format to the uploader and persists the reserved Facebook video id before uploading, and `ChannelExpired` parks an episode on a dead-token channel instead of failing it — the video waits in the buffer for the next slot (ADR-073); `_fail_task` scrubs the stored traceback (it is rendered in the UI and the bell, and a Graph URL carries the token) and retires the channel on a publish OAuth failure — the one thing that ever writes `ChannelStatus.expired` (ADR-072); render_task (buffer + continuous/slot-scheduled/review; persists the generated script into `render_json.script` and reuses it on retry — resume rebuilds the SAME episode with no second script call; the success path's render_json overwrite consumes the checkpoint; `drop_script_checkpoint` clears it on reject/discard so a reroll genuinely rerolls — ADR-069) + publish_task; Auto-QC gate (batched footage vetting, deterministic black/silence checks + judge master, re-render once, else park); failure circuit breaker (3 consecutive fails → campaign `failed` + one alert; ▶ Start or a later success resumes); records published A/B variant + the scene map (`render_json`) on the Task; daily render cap; duration + affiliate + prefix + script_depth + video_format + visual_source/visual_style/characters + separate image model pass-through (Studio Mode needs no Pexels key — `_resolve_keys` only requires it for stock; binds the Pollinations token + geometry into the image generator — ADR-053; rolls the per-episode Vibe (`vibe.roll`) for quote campaigns, forcing Studio visuals + jittering the voice + routing Auto music through the mood — ADR-056; `_cast_with_ref_urls` attaches each character's PUBLIC reference URL so kontext can honour an uploaded image — ADR-055); per-channel footage dedupe (loads recent clip ids → `recent_clip_ids`, records `used_clip_ids` after render) + episode-seeded motion; persona+memory, synopsis store (title fallback — never empty); music config-truth (auto without FREESOUND_API_KEY fails loudly); per-user Gemini model chain honored across generation/vetting/final QC; state machine, error→Telegram; shared review actions `apply_approve`/`apply_reject` (delete+relearn+optional re-render) used by BOTH the manual Review routes and the autopilot reviewer (DRY); `apply_approve` releases the buffer row to `ready` and marks the Task SCHEDULED so an approved episode leaves the review queue instantly instead of reading as approved-and-awaiting-review at once (ADR-064); hydration treats CANCELLED as a finished outcome; `apply_reject(automatic=True)` charges an autopilot rejection to the autopilot's own re-render budget rather than the operator's retry count (ADR-076); fresh scripts pass the slop gate before any render (ADR-079): block → ONE regenerate with the issues as avoid-notes (flop_notes join reject_reasons in the avoid list) → an honest non-transient failure with no checkpoint; warnings ride into review metadata; `_recent_fingerprints` reads finished episodes' narration/title + in-flight checkpoints; `compile_task` builds a best-of under the render lock (stream-copy concat + chapters + poster thumbnail) and ALWAYS parks it for review; publish retains the master into the library and a compilation never advances the campaign; `enqueue_task` routes every Retry by `video_kind` (a compilation re-concats — render_task would try to script it); hydration ignores sentinel episodes (ADR-082); a compilation's rejection never becomes a script avoid-note (editing complaints must not steer the scriptwriter); QC no-verdict routing (ADR-084): an unavailable judge never burns the re-render, parks for review by default (`qc_failopen: publish` opts out — but failed-then-unavailable ALWAYS parks), `requalify_task` re-judges a parked item in place; every render persists a `journey` timeline (script/gate/judge/QC per attempt) into `render_json` for the episode page; R22 (ADR-088): render completion commits the buffer row AND the task's terminal state in ONE transaction — no more crash window that produced 'FAILED banner over a finished QC-passed video'; publish success is likewise one atomic commit (published ids + consumed + COMPLETED) and publish failure keeps the buffer `ready` (a failed upload does not un-approve) with a publish-labeled error; `publish_task` accepts ONLY `ready` — approval is the one gate onto the platform; publish jobs write no stall-progress entry and persist `publish_attempts` before bytes go up, arming the platform duplicate check on every re-publish path; `apply_approve` verifies the file + no in-flight job (`ReviewConflict`), stamps `ready_at` + durable `publish_requested_at`, and counts approving a FAILED episode as its retry; `_fail_task` stores the exception summary first and journals the attempt; `_mark_channel_expired` is platform-aware (YouTube `invalid_grant` → `token_definitely_dead` probe); the circuit-breaker streak skips infrastructure/reject rows; `advance_campaign` activates the next campaign only on the TRANSITION into completed and clamps the counter; render/compile carry idempotency guards (`_job_is_stale`); AI-phase heartbeats between Gemini steps keep a slow-but-alive script phase off the watchdog's radar. ADR-089: `_fail_task(journey=…)` persists the decisions an attempt made before it died (the journey was only written on the success path, so a gate-failed episode reported "1 decision" having made four), and a judge refusal is now attributed to "Script judge" instead of "Script gate"; `_remember_gate_failure` records WHICH brief version the gate refused (committed, because `_fail_task` rolls back first; the resume checkpoint is dropped so a Retry writes a fresh script); catchphrases come from `creative_brief.active_catchphrases` and reach the gate and the judge, not just generation; gate objections are PREPENDED to the avoid-notes (the 10-note cap silently dropped them on any campaign with a full reject list, so the regenerate re-ran the failing prompt); a retry after an edit is told the brief moved; `_mark_channel_expired` also fires on a live-but-unpostable Page token, still verifying before condemning; `apply_approve` SCHEDULES rather than uploads when the campaign has posting slots and `approved_publish_time` reports which slot it took — approval is a gate, not a publish trigger (ADR-090) | Task/Buffer id | published video, DB updates | video_factory, qc, services, models | 2026-08-05 | 2026-08-06 |
| `run_worker.py` | worker | Entrypoint: one `SimpleWorker`, warm SIGTERM shutdown, job_timeout; clears the crash-artifact render lock + live-progress entries + any stale restart flag at boot, then fails the DB rows those artifacts belonged to via `scheduler.fail_orphaned_renders` — a render abandoned by an operator restart / redeploy / crash is failed (and so retryable, and so resumable) immediately instead of reading "Rendering 47%" for up to 2 hours, ADR-070; starts the scheduler AND watchdog threads; R22: `FactoryWorker` whose death penalty raises `JobHardTimeout(BaseException)` — the pipeline's broad `except Exception` retry/fail-open handlers can no longer swallow the one-shot SIGALRM and run the job unbounded | — | running worker | task_queue, scheduler, watchdog, rq | DONE | 2026-08-04 |
| `workers/watchdog.py` | worker | Self-recovery daemon thread (ADR-057): every minute checks the operator restart flag and progress-staleness; a render with no progress for `JOB_TIMEOUT + WORKER_STALL_GRACE` (i.e. one that outlived its own RQ timeout ⇒ the worker loop stopped executing) is marked FAILED with an actionable message, its progress + render lock are released, the operator is alerted, then the process exits so compose recreates the container. Deliberately does NOT feed the campaign circuit breaker (infrastructure ≠ bad campaign config); R22: reconciles a finished-but-unrecorded episode (workers/reconcile.py) instead of failing it; PUBLISHING is no longer stall-failable — uploads answer to their own RQ timeout, not the render stall limit that sat below it; probe reads/lock release use the bounded `probe_conn` | Redis progress stamps, restart flag | failed task, process exit | task_queue, models, video_worker | DONE | 2026-08-04 |
| `workers/reconcile.py` | worker | One answer to "did this episode's work actually finish?", asked BEFORE anything writes FAILED (R22, ADR-088): `reconcile_task_outcome` repairs a task whose status lags its committed evidence — `published_video_id`/consumed buffer ⇒ COMPLETED (never re-render a live upload), live `awaiting_review` buffer ⇒ AWAITING_REVIEW, live `ready` buffer ⇒ SCHEDULED; called by the watchdog, boot recovery, the stuck-task reaper, both retry paths and the hourly self-heal; CANCELLED is never touched (an operator's decision stands); `live_buffer` is the shared file-really-exists lookup | Task | repaired status | models, types | DONE | 2026-08-04 |
| `workers/scheduler.py` | worker | Daemon tick: `fail_orphaned_renders` — boot recovery called from `run_worker` before the worker consumes anything (ADR-070: at boot the single worker is provably not rendering, so any task still in a working stage is the previous process's abandonment — failed, not re-queued, so the retry cap holds and the autopilot resumes it); autopilot retry = the CONTINUE for interrupted renders (ADR-069/070: ACTIVE campaigns only — it was re-queueing the episodes the circuit breaker had just stopped a campaign for, and re-publishing for completed ones; transience gate via `core.failure.is_transient`, ghost progress cleared, checkpoints of recently-failed tasks — `resume_checkpoint_ids`, 24h — excluded from the orphan sweep except under disk pressure); stuck-task reaper (clears ghost progress) + orphaned render-lock clear (two-tick, render-concurrency-safe; the live-render guard is factored into `_render_appears_live`, shared with the operator-triggered `recover_now` behind the Operations page's Recover button — same guard, no two-tick wait since an explicit click is the confirming signal — ADR-058), eager hydration, slot-timed publishing with weekday gate (`posting_days`, stretched expiry for day-gated buffers) that honours a per-episode `publish_at` override first (`due_override_item` — an operator-set time outranks the slot gates; a FUTURE override is excluded from both the slot pick and catch-up so a reschedule is never undone — ADR-059), buffer expiry, disk sweep, daily learning pass (feeds retention drop-off notes into the distiller) + a daily channel-growth sample riding the hourly stats pass (`collect_channel_snapshots`, self-throttling — ADR-063) + min-per-day watchdog + operator heartbeat digest; hourly stats pass (`hourly_stats_pass` — early views + retention refresh, moved off the daily tick so first-retention isn't stacked on the daily cadence) (AI-budget line uses each user's `settings_json` budget, else the env fallback); autopilot pass (`autopilot_pass` — per-channel cadence via a Redis NX guard: AI review of awaiting-review renders from their stored QC verdict — auto-reject weak/failed renders with a learning reason + re-render, auto-approve+publish strong ones in full-auto / recommend in copilot, escalate the middle; auto-retry genuine render failures skipping quota + operator rejects; catch-up publish of missed slots; files strategy proposals into the AutopilotAction inbox (`autopilot_propose_channel`, idempotent) and applies approved ones (`apply_autopilot_action` — extend/wind-down mutate `total_episodes`, tune mutates caption/music/rate, successor carries the proven formula into a fresh campaign (AI-designed angle via propose_campaign, fail-open to a plain clone — S5); never deletes); full-auto auto-applies structural proposals with guardrails (`autopilot_autoapply_channel` — `max_active` cap, one successor/pass, successors start review-first "training wheels"; creative tunes stay operator-confirmed); weekly strategist (`autopilot_strategist_channel` — one AI call, guarded by cadence + Gemini key + daily-budget reserve, targets the WEAKEST measured campaign with its retention drop-off notes in scope, files a suggest-only tune — S4/S6); Telegram summary + a per-channel run heartbeat (`last_run` time+summary → the UI shows 'last ran Xh ago' / 'never ran'); every autonomous decision (approve/reject/escalate/retry/catch-up) is logged as a done AutopilotAction with its reason+evidence (`_log_action`), pruned after 90 days (`prune_autopilot_log`)); ADR-076: `_ap_step` isolates each autopilot step so an optional AI call cannot cancel review/retry/catch-up (and a partial pass shortens the cadence key instead of banking the full interval it never earned), `AUTOPILOT_MAX_REJECTS` + `_auto_rejects_spent` bound the auto-reject→re-render loop that nothing else bounded (the circuit breaker never sees it) and escalate past the cap, `_channel_can_act` stops an expired-token channel spending the single render slot on videos it cannot publish, the retry budget reads `auto_retry_count`, `_record_heartbeat` re-reads the row before its read-modify-write, and the hourly stats pass is handed UTC explicitly (the tick's `now` is LOCAL); `check_monetization_milestones` (daily pass) announces 80%/100% of each threshold once per level (state in `autopilot_json.milestones`), phone ping only at 100% (ADR-080); `autopilot_council_channel` runs the daily judged pass (guards: Gemini key, 80% budget reserve, once per UTC day) as an isolated `_ap_step`, `apply_autopilot_action` gained the reversible `slot_change` branch (re-checks the slot still exists at apply time), and full-auto may auto-apply slot changes — bounded by the rails' weekly cooldown (ADR-081); `autopilot_requalify_channel` re-judges no-verdict parks once each, budget-guarded (ADR-086); runway-aware buffer expiry (an item's max age knows its slot queue position and `publish_at`, ADR-087) with feed-visible expiries; `finish_stranded_campaign` completes a campaign no automatic retry can save and activates the next; `log_council_report` shared with the Run-council-now button; quota-failed renders retried after the Pacific reset; publish-only retry when the video already exists; R22 (ADR-088): reap/boot reconcile-before-fail via workers/reconcile.py (+ upload-specific failure copy, orphaned-job cancel, genuinely-queued-backlog exemption); `reconcile_stranded_episodes` hourly self-heal — repairs FAILED rows whose work survived and re-issues lost publish jobs once the channel is active again; autopilot retry decides from the surviving buffer BEFORE mutating the task: `ready` ⇒ publish-only retry (task SCHEDULED, job id recorded), `awaiting_review` ⇒ restored to Review and NEVER auto-published, already-published ⇒ finalized; review pass scans active campaigns only, skips episodes with an in-flight job, and escalates vanished files / once-failed uploads instead of looping approve→fail; expiry ages from `ready_at`, spares items with a queued publish and channels waiting on a fresh token, and writes the honest aged-out message. ADR-089: `retry_after_brief_edits` re-queues gate-failed episodes whose campaign's creative brief has been EDITED since — the gate's "a human must change something" was right and nothing noticed when the human did, so episodes stayed dead under a brief that no longer existed. Deliberately outside the autopilot pass and its budget (the trigger is the operator's own edit, not a machine judgment, and it applies to hand-driven channels too), skips channels that cannot publish, never publishes, and grants exactly ONE attempt per brief version by stamping the fingerprint that bought it; `slot_scheduled` is THE definition of "a schedule owns this campaign's timing" and `upcoming_slots` THE projection of when it next posts, both independent of `auto_publish` so review-first campaigns keep their slots and posting days (ADR-090) | active campaigns | jobs, cleanup, playbooks, alerts | video_worker, autopilot, analytics_service, ai_engine, usage | 2026-08-05 | 2026-08-06 |
| `services/youtube_service.py` | publish | OAuth2 token refresh (persist to channel) + resumable upload (declares `defaultAudioLanguage`/`defaultLanguage` from the campaign language — country/audience signal, ADR-045) + CTA comment; `add_to_series_playlist` (ADR-080): one playlist per campaign, created once + id cached on the campaign config, one recreate on a stale id, fail-open end to end — session-time signal and watch-hours toward the money threshold; `find_existing_upload` — retry idempotency by exact title over recent uploads, parity with the Facebook guard (ADR-087); `token_definitely_dead` probe — verify-before-condemn for YouTube auth, ADR-083 parity (R22) | video, metadata, channel | uploaded video id | google-api-python-client, google-auth | DONE | 2026-08-04 |
| `services/facebook_service.py` | publish | Publishing at parity with youtube_service (ADR-073): a vertical short goes up as a REEL (`/video_reels`, three-phase start→transfer→finish — which is also what makes the transfer RESUMABLE and reserves the video id before any bytes move), long-form stays a Page video; the campaign's privacy choice is honoured (public→PUBLISHED/published, anything else→DRAFT — Facebook has no "unlisted"), the CTA is posted as a comment, `permalink` builds a URL that actually resolves (`/reel/{id}` · `/watch/?v={id}`), and `find_existing_upload` stops a client-side timeout from posting a second copy. Also owns the Graph constants + error semantics for the WHOLE Facebook surface (ADR-072): one `GRAPH_VERSION` / `GRAPH` / `GRAPH_VIDEO` (the version was hardcoded in four places), `raise_for_graph` — Facebook's own `error.message` instead of "400 Bad Request", token-scrubbed — and `FacebookError`/`FacebookAuthError` (OAuth 102/190/463/467 → callers retire the channel, and `core.failure` reads the wording as a non-retryable credential fault), plus `scrub` for anywhere a Graph string could reach a log, the DB or the UI; ADR-083: auth is decided by `error.code` alone ({102,190,463,467}) — Graph stamps "OAuthException" on rate limits (4/17/32/613) and temporary errors too, and that blanket rule falsely retired healthy channels hourly; transient codes raise `FacebookError(transient)`; `token_definitely_dead` is the one second-opinion both retirement sites must ask; `find_existing_upload` adopts a pending id only when the BYTES landed (uploading_phase complete / video_status ready — a reserved-but-empty Reel re-uploads, ADR-085) | video, metadata, channel | uploaded video id | requests | DONE | 2026-08-04 |
| `services/telegram_bot.py` | publish | DRY alert helper (queued/finished/failed) to a user's chat | message, token, chat id | Telegram message | requests | DONE | 2026-07-17 |
| `services/verification.py` | publish | Live credential checks (Gemini/Pexels/Telegram/Freesound/Pollinations) for the "Test" buttons (Pollinations draws one tiny image — keyless is valid), plus the Facebook Page gate (ADR-068/072): `check_facebook_page` asks `/me` — with a PAGE token that IS the Page, and only a Page carries `category`, which is how a short-lived USER token (the commonest mistake, and one that reads a Page's public name perfectly) is finally refused; it also rejects a token belonging to a DIFFERENT Page, and returns a `PageCheck` — three-state verdict + canonical numeric id + name + picture — so the save can store and show the truth; a local pre-check names the commonest slip — the Page ID pasted into the token box — before spending a call, because Graph answers that with the unhelpful "Cannot parse access token" (ADR-074); plus `normalize_page_id` (a pasted URL / @handle / username becomes a usable id). Never raises, never surfaces the token-bearing request URL; ADR-077: `check_facebook_page` identifies the node with Graph's own `metadata=1` introspection and asks only for fields EVERY node type has — asking for `category` (Page-only) made Graph refuse the whole request for a User token, so the branch that names that exact mistake was unreachable and the operator read “(#100) Tried accessing nonexisting field” instead; `_wrong_node_kind`/`_not_a_page` translate a #100 into what it means (lifting the node type out of Graph's own text), a direct `category` probe is the fallback, and an unidentifiable token is “could not tell”, never “verified”; a rate-limited check returns "could not verify right now — NOT rejected" instead of a definite rejection (ADR-083). ADR-089: `missing_post_permission` asks Graph's own `debug_token` whether a Page token may actually POST — identity was checked and capability never was, so a read-only token saved as "● Active", passed every re-verification, and refused every publish weeks later with "#200 … lack of pages_manage_posts"; three-state like its sibling (an absent scope list is not evidence of no permissions), and a confirmed missing scope is a DEFINITE refusal at save time | keys | (ok, detail) | requests | 2026-08-05 | 2026-08-04 |
| `services/analytics_service.py` | learn | Two-speed stats into Task.stats_json: `collect_early_stats` (near-real-time views/likes via the YT **Data** API for young videos, ~hourly, own `early_fetched_at` clock, never sets retention) + `collect_stats` (YT Analytics retention/views, FB insights), plus views-by-country (`fetch_youtube_geography` → top_country + share) for audience-match verification (ADR-045) and the second-by-second retention curve (`fetch_youtube_retention` → `elapsedVideoTimeRatio`×`audienceWatchRatio`), attributed to the scene that lost viewers (`core.retention`) as a stored drop_summary; Facebook insights are ONE batched Graph call instead of up to fifty, with per-entry status so one unreadable video does not discard the rest (ADR-073); Facebook calls go through the shared `GRAPH` + `raise_for_graph`, and a dead Page token retires the channel mid-pass instead of failing hourly forever (ADR-072); channel-level growth (ADR-063): `fetch_channel_totals` (YouTube `channels.list` statistics — honours `hiddenSubscriberCount` as None, never 0 — / Facebook Page followers, views left None rather than faking an equivalent), `collect_channel_snapshots` (one row per channel per local day, checks the day row first so extra ticks cost no API quota, best-effort per channel) and `channel_growth` (per-day sub/view deltas beside episodes published that day — the correlation view; a first sample yields None deltas, never 0); ADR-076: `collect_stats` MERGES into `stats_json` instead of rebuilding it — a rate-limited geography call, or an episode past the per-video curve cap, used to delete the retention curve, the scene drop attribution and the top-viewer country that were already measured; curves are only requested for episodes that lack one (one HTTP round trip each, sequential, in the scheduler thread); `_capped` logs every truncation and the due list is sorted never-measured-then-oldest so a cap drains instead of starving a busy channel's tail; `scope_problem`/`_note_analytics_health` turn a permanent 403 into `Channel.analytics_error` the Channels card and the alert feed can act on; `channel_growth` reports `samples` and a real calendar `days` span separately; the early pass stamps `views_24h` once past 24h and judges flops per campaign (`judge_flops`), and the retention pass adds the late hook autopsy for flopped episodes whose curve blames the opening (ADR-079); watch TIME is collected everywhere views were (ADR-080): `estimatedMinutesWatched` rides the same per-video Analytics query, FB's batched insight call adds `total_video_view_total_time` (ms→minutes), and `fetch_youtube_monetization_windows` stamps the YPP trailing windows (365d minutes, 90d views) onto each daily ChannelSnapshot; flop judging covers ORDINARY episodes only (ADR-085) | channels, video ids | stats_json, ChannelSnapshot rows | googleapiclient, requests, retention | DONE | 2026-08-04 |
| `services/music_service.py` | render | Auto background music: random CC0 track by mood via Freesound API (niche mood → one generic-query retry), local cache, per-episode credit | mood, api key | mp3 path + credit | requests | DONE | 2026-07-21 |
| `main.py` | web | FastAPI app: login/session, dashboard (health/counts + AI-quota chip), campaigns CRUD (create incl. one-click start_now)+duplicate + tabbed campaign hub (`GET /campaigns/{id}` Overview = per-A/B-variant retention summary + scorecard + playbook, `/campaigns/{id}/episodes` stage-tabbed episode list via the shared `_episode_list_ctx`, `/campaigns/{id}/settings` edit form; legacy `/campaigns/{id}/performance` + `/edit` URLs 307-redirect into the hub), AI campaign designer endpoint (downgrades auto-music when no Freesound key; forwards the form's video_format so Long campaigns are designed long-form), asset actions (approve / reject→learning / publish-now / discard&re-render), streaming, task API+retry, credential tests (incl. Freesound), Gemini model-chain save + live model listing endpoint, OAuth; per-user Settings page (`/settings` — new-campaign defaults that seed the New Campaign form via `_new_campaign_defaults` + AI daily budget powering the dashboard quota meter & Telegram heartbeat, stored in `users.settings_json`, keys stay on Credentials); per-channel autopilot config route (`POST /channels/{id}/autopilot` — mode/cadence/review thresholds into `Channel.autopilot_json`); per-channel profile route (`POST /channels/{id}/profile` — validated audience/language/timezone/voice/style/vision into `Channel.profile_json`); the New Campaign form seeds language/voice/timezone from the selected channel's profile (profile > Settings) and re-localizes client-side on channel switch; AI Propose forwards the channel so the design is localized; the campaign-hub Overview shows an audience-match line (measured top-viewer country vs the channel target); autopilot decisions inbox + mission-control page (`GET /autopilot` — per-channel run-status strip, proposals, and a paginated activity feed of every logged decision; `POST /autopilot/{id}/approve`; R22: `retry_task` retries the failed STEP and never publishes unreviewed content (`ready` ⇒ publish-only + flash=publish, `awaiting_review` ⇒ back to Review + flash=review, already-published ⇒ finalized + flash=published); approve route surfaces `ReviewConflict` as 409; one problem per episode — `_task_counts`/triage/bell exclude FAILED tasks whose video sits in the review queue (buffer wins, same tie-break as the stage chips); episode view passes `render_survived` (Failed banner explains the video below is complete), `will_auto_publish` (a ready buffer still goes out at the next slot) and the shared stage vocabulary for the pill |`/dismiss` — approve applies via the scheduler, dashboard triage surfaces the pending count); read-only /api/summary snapshot (header attention badge + dashboard auto-refresh, reuses the dashboard helpers; also returns the open-autopilot-proposals count via the shared `_autopilot_proposed_count` helper, feeding an Autopilot sidebar badge); master-detail drill-down (scoped list views via ?channel=/?campaign= query params on campaigns/assets/tasks + rollup counts for the Channel→Campaign→Assets links, `/campaigns/{id}` is the campaign hub with Overview/Episodes/Settings tabs); dashboard triage inbox + activity feed + factory scorecard (7-day throughput, buffer runway, week-over-week retention) + next-publish context; per-campaign operational snapshot (`_campaign_ops`: in-flight render+progress, queued, ready/awaiting-review buffer, `_next_slot`) shared by the campaign cards, hub header and dashboard "Running now" panel; week-planner calendar (`?week=` navigation; per-slot fill state via `_calendar_row_cells` — assigns ready buffer episodes lowest-first to upcoming slots so each cell shows ● will-publish + projected Ep / ○ will-be-missed / dimmed past / — gate; today's column highlighted; rows carry channel + format + link to the hub); server-side pagination + status-filter chips on the Asset Pool (whole-scope per-status tallies); the campaign-hub Overview aggregates (variant summary, retention trend, best-episode scorecard) compute over the full episode set while the hub Episodes tab paginates the list; /api/tasks paginated + searched (?q over id/status/topic/channel) + scoped (?campaign) in SQL so the live Task Logs feed can page through the full task history (newest first) instead of a fixed 50-row window; /api/search read-only global search across channels/campaigns/episodes for the ⌘K palette; content-hashed static_url() + immutable far-future cache headers (CachedStaticFiles) for versioned static assets; /episodes pipeline list — every episode as one row grouped by lifecycle stage (Queued/Rendering/Review/Scheduled/Published/Failed tabs with counts + search + scope + pagination), unifying Task Logs + Asset Pool → the detail view; /episodes/{task_id} Episode view — one home per episode (lifecycle timeline + preview + stage-aware actions that reuse the asset/task routes via a safe `return_to`); one filter grammar — server-side status chips (true counts) + search on Campaigns / Channels / Asset Pool (chips scope-based, search + status narrow the results + paging count); persistent channel scope switcher (sidebar select via the `nav_channels` global; scopes Episodes/Campaigns/Assets/Task Logs/Calendar — `/tasks`+`/api/tasks` and `/calendar` honor `?channel=`; localStorage-sticky so it survives visits to the factory-wide Dashboard/setup pages; scoped chip counts); Studio Mode (ADR-052/054): per-channel character-cast CRUD (`POST /channels/{id}/characters` + `/characters/{cid}/delete` — validated via `_sanitize_characters`) with optional reference-image upload/replace/serve (`/characters/{cid}/image` GET+POST; re-encoded via PIL, ≤15 MB, opportunistic HEIC, explicit save-ok/save-fail flash feedback — W4) + a PUBLIC token route `GET /studio/ref/{token}` (no auth, hex-only, random-token filename — so Pollinations kontext can fetch an uploaded reference, ADR-055), campaign `visual_source` stock|studio + `visual_style` + `title_overlay` + `content_style` story|quote + `signature` in `_build_campaign_config` (ADR-056), and a separate `gemini_image_model` on the Credentials save route (image model managed apart from the text model) + an encrypted per-user `pollinations_token` + a `pollinations` "Test" (ADR-053); Operations page (ADR-058): `GET /operations?tab=queue|worker` — the factory floor, so a frozen render is diagnosed and fixed from the browser instead of over SSH (`_ops_job_rows` = queued jobs in true RQ order joined to their episode + a count of the upload jobs sharing the queue, tenancy enforced through the Task row; `_ops_worker_card` = the single worker's verdict down|stalled|busy|idle from the same `worker_alive` the health strip uses, its live render + progress-staleness, render-lock state, pending restart) with four actions: `POST /operations/jobs/{job_id}/front` (render next — RQ `at_front`, same Job so `rq_job_id` stays valid) · `/cancel` (drop from the queue, Task→FAILED so Retry still works) · `/operations/recover` (on-demand `scheduler.recover_now`) · `/operations/restart-worker` (Redis flag only — the internet-facing web container never holds the Docker socket); the 🚀 Publish-queue tab (`_ops_publish_rows` — every rendered episode with WHEN it goes out: an operator override, a projected slot (ready episodes assigned to the campaign's next free slots lowest-first, the scheduler's own rule, via the shared `_upcoming_slots` that `_next_slot` now also reads — one definition of "next post"), or "after you approve it"; flags rows whose file left the disk) + `POST /operations/buffer/{id}/reschedule` (a browser `datetime-local` read in the CAMPAIGN's timezone via `_campaign_tz`/`_to_campaign_tz`, stored as naive UTC; empty clears the override — ADR-059); ONE scheduling surface (ADR-067): `/calendar` is "Publishing" with a **Week grid; Settings saves `slop_blacklist` (≤50 lines, merged with gate defaults at check time — ADR-079). ADR-089: a campaign edit and a change to the channel's script-judge threshold write a `config_edit` audit row (digests/changed key NAMES only — the persona and system prompt stay out of the audit table), which is what the brief-edit retry log entries refer back to; the episode page also gets `retry_helps` (so "fix the cause, then Retry" is not printed over a failure no retry can clear) and `brief_edited` (so an episode already on its way back does not read as dead) | List & actions** toggle — the list view absorbed the Operations publish tab (`?tab=publish` 301s there), `_calendar_row_cells` now consumes the shared `_upcoming_slots` instead of re-implementing the scheduler's assignment rule (the only duplicated business rule in the codebase, and it had drifted), and operator-set publish times are drawn on the grid as ✏ chips and counted in the ready column; a dashboard cut to four blocks (ADR-066): `_campaigns_with_empty_buffer` (runway leads with the WORST campaign, not an average that hid two about-to-be-missed slots) + `_activity_feed` (collapses runs of identical consecutive events — a publish burst was ten near-identical lines); ONE episode list (ADR-065): the `/tasks` PAGE is retired to a 301 into `/episodes?status=rendering` (`/api/tasks` stays, gaining `live=1` for the in-place row updater), every stage chip filters in place, and the **Review stage is derived from the buffer** (`_review_episode_keys` + `_apply_stage_filter`/`_stage_counts_from`) so the chip equals the attention badge by construction and an episode belongs to exactly one stage — the chip used to read "Review (0)" while two videos waited, because a Retry had moved the task on while its buffer row still said `awaiting_review`; one ordering (`updated_at` desc) for both episode surfaces so actionable work is never buried under published rows; ONE attention count shared by every badge (`_attention_count` = failed + awaiting-review + open proposals, served on `/api/summary` and `/api/alerts` so the bell, hamburger, rail and triage pill can never disagree — ADR-064); factory-wide vitals for the dashboard (`_factory_vitals` — cumulative views reported WITH the number of measured episodes since analytics lag ~2 days, all-time published, today's finished-render failure rate + machine minutes from `render_json.render_seconds`, plus `core.host` CPU/memory; `_server_day_start_utc` derives "today" from the server zone via `_campaign_tz(None)` so the dashboard and a campaign agree — ADR-062); per-channel growth on the Channels page (`analytics_service.channel_growth` per card); the cross-channel alert feed behind the header bell (ADR-060): `_alerts` merges four fail-soft sources — `_infra_alerts` (Redis/worker down or wedged, disk pressure, AI quota ≥80%), `_work_alerts` (per-episode failures with the last stack-trace line, breaker-paused campaigns, review + autopilot backlogs as counted aggregates), `_schedule_alerts` (a slot within 6h with an empty buffer — warned BEFORE it is missed) and `_success_alerts` (recent publishes, green) — sorted red→amber→green and served by `GET /api/alerts` with an `actionable` count that is the badge number by construction; `_system_health` gained `worker_stalled` (registered-but-wedged is its own signal); `_safe_return` allow-lists `/operations` so the shared asset actions bounce back there; ; onboarding survival (ADR-068): `_setup_state` (the three first-run steps, hoisted to the top of the dashboard until done — `api_keys` not `keys`, since Jinja resolves `setup.keys` to the dict's own method), a `google_oauth_start` guard that explains the missing server-side OAuth client instead of redirecting to Google with `client_id=None`, `add_facebook_channel` verifying the Page through `services.verification.check_facebook_page` before storing it (only a definite rejection blocks the save), `_credential_alerts` (an ACTIVE campaign whose required key is missing — Pexels only for campaigns that actually use stock footage), `_diagnose_failure`/`_FAILURE_PATTERNS` (a recorded render error → cause + fix + somewhere to act, shared by the episode page, the dashboard triage row and the bell; returns None rather than guessing), the first campaign defaulting to Review-first unless Settings says otherwise, an HTML error page for browser navigations (`_ERROR_COPY` + `error.html`; API callers keep the JSON), and `/api/search` matching accent-insensitively via `_fold` (`đ`→`d` by hand — NFD leaves it) with `_EP_QUERY` parsing "ep 3"/"tap 3" to an exact episode number; failure diagnosis now aliases `core.failure` (one table with the autopilot — ADR-069), a refused Facebook connect redirects to `#fb-form` carrying the operator's non-secret values back (never the token) so the error and the form share a screen (ADR-074); `_safe_avatar` keeps a non-https URL out of the channel card's `<img src>` (ADR-073); the Facebook connect flow verifies before it trusts and only says "verified" when it was, stores the canonical Page id, offers the Page's own name/avatar, and `POST /channels/{id}/facebook-token` replaces a rotated token WITHOUT deleting the channel (only a verified one clears `expired`); `_credential_alerts` reds an expired channel (ADR-072); the campaign config gained `voice_delivery` (normal|soft) and its grade whitelist now reads the one catalog, `/campaigns/propose` forwards `content_style` so a quote design gets soft voices (ADR-071); the Settings page saves `image_timeout_s` (30–600, clamped) and Discard & re-render drops the resume-script checkpoint (a reroll must reroll); `POST /assets/{id}/run-qc` re-judges a parked no-verdict item in place (enqueue_requalify, flash `qc_queued` — ADR-084); campaign form gained `qc_failopen` (review/publish) + `script_judge` (on/off); `POST /channels/{id}/council-now` (daily gate bypass only — reserve + cache still apply), autopilot approve/dismiss take an allow-listed `return_to`, the hub Overview carries the campaign's proposals/actions and the calendar carries pending slot-change chips (ADR-086); template globals: settings + voice_choices + static_url + query_string + nav_channels; approving a slotted campaign's render redirects with the scheduled time rather than "publish queued", and the calendar/publish list/next-slot chip no longer hide review-first campaigns (ADR-090) | HTTP | HTML/JSON | fastapi, auth, models, task_queue, scheduler, ai_engine, tts, usage | 2026-08-05 | 2026-08-06 |
| `templates/` | web | Jinja2 dashboard (7 pages incl. Calendar) + login.html, built on `macros.html` (pill/stat/progress/bar/banner/empty/field/pager/scope_note/stage_tabs + shared `filter_bar` — chips + server search, URL-driven, used across Campaigns/Channels/Asset Pool) and one `stage_tabs` bar (All/Queued/Rendering→log/Review→workbench/Scheduled/Published/Failed + counts via `_episode_stage_counts`) shared by /episodes + /assets + /tasks — ONE control, no lateral loop; shared `pager`/`scope_note` macros (DRY across the paginated + scoped lists); the base shell (destinations-only rail Monitor/Build/Setup — render log + Review are facets of Episodes, Calendar a view of Campaigns; two-level active state lights the parent on child routes; ONE mobile nav — bottom bar mirrors the rail + a More button opening the same drawer; sidebar channel scope switcher, ⌘K global-search palette). Persona-ordered pages with full empty/loading/degraded states; `episodes.html` pipeline list (stage tabs + search, rows → the detail view; absorbed the retired render-log page — its worker-down banner now follows the Rendering filter, `_episodes_table.html` rows in a working stage carry `data-live-task` so `app.js` moves their pill/progress in place, and the Review filter offers the workbench rather than a chip silently teleporting there; a dense 2-line mobile row took 38 episodes from ~8.9 phone screens to ~3.5) + per-episode `episode.html` (lifecycle timeline Queued→…→Published + preview + stage-aware actions, linked from Episodes/Task Logs/Performance); Asset Pool video-first review cards + flash banners; guided campaign form (AI Propose — sends the chosen video_format so Long is designed long-form — + script Preview, per-language voice dropdown, format-aware video-length inputs (short ≤180s / long ≤900s), script-depth standard/deep selector, progressive-disclosure advanced sections, sticky save bar, JS validation with tab-jump); campaign-hub Overview A/B retention bars + best-episode scorecard; Credentials Gemini model-chain picker (live RPM/TPM/RPD list); accessible confirm dialog (data-confirm, optional type-to-confirm for channel delete); cross-page live attention badge + mobile bottom tab bar; Review (Asset Pool) server-side status-filter chips + pagination + reject-reason quick-pick + keyboard review (J/K/A/R), cards link to the episode detail; campaign hub with server-rendered tab bar (Overview/Episodes/Settings) via shared `_campaign_hub.html` (breadcrumb + header + Start/Duplicate/Delete actions), Episodes tab reusing the shared `_episodes_table.html` partial (also used by the global `episodes.html`), Settings tab wrapping the campaign edit form; the hub Overview leads with a "Now & next" strip (rendering/queued/ready/next-post + schedule facts + a pending explainer) and folds the playbook + A/B variants + retention trend into one "Learning & results" card with a single empty state; campaign cards + every cross-page campaign link (breadcrumbs on Episodes/Review/Task Logs, the Episode view's "Campaign ↗", dashboard feed) open the hub `/campaigns/{id}`, and the card is decluttered to Open →/Start/Delete with a "N awaiting review" signal; the New Campaign form follows the scoped channel + seeds language/format/publish/slots/episodes from the Settings defaults, Duplicate preselects the source channel, "Create & Start" reliably starts (submit carries the clicked button through), and Channels' "Add a Facebook Page" is a collapsed disclosure; a `settings.html` preferences page (new-campaign defaults + AI daily budget); per-channel Channel Profile editor + summary chips on the Channels page (audience/language/timezone/voice/style/vision) that seed campaigns and localize AI Propose; per-channel autopilot control on the Channels page (mode Off/Copilot/Full-auto + cadence + QC review thresholds), an "🤖 AI recommends" hint on Review cards, and the `autopilot.html` proposals-inbox + decision-log page (each proposal shows the data evidence + Approve/Dismiss); operations-first campaign cards — shared `sched_facts`/`now_next` macros render format+schedule chips and a live "▶ Rendering Ep N · %" / "⏭ Next post" / "⚠ Buffer empty" line, sorted active→pending→failed→completed with actions pinned to the card bottom (fed by `_campaign_ops`); paginated + server-searched Task Logs (whole-history, newest first, AWAITING_REVIEW rows link to the episode); campaign actions preserve the channel scope; dashboard triage links to the episode detail; Performance retention sparkline; "Create & Start" one-click; master-detail navigation (breadcrumbs, drill-down links Channel→Campaign→Assets/Tasks, scoped list views, campaign-hub tab bar); "trust-instrument" dashboard (triage inbox + inline retry, all-clear state, a "Running now" per-campaign panel — one row per active campaign with its live render/next-post state + Open/Review links, activity feed w/ since-last-visit, factory scorecard: 7-day throughput sparkbars + buffer runway + retention trend + next-publish; the health strip is infra-only, buffer count lives once in the scorecard runway); review-in-context criteria on Asset Pool; visible learning loop; live campaign identity card; Studio Mode UI (ADR-052): per-channel "🎭 Studio cast" manager on Channels (add/remove consistent characters, diverse example presets, optional reference-image upload + preview — W4), campaign "Visual source" + art-style override (with presets) + "Billboard title" (ADR-054) + "Content style" story; R22: episode.html — truthful failed banner when the render survived (approve/publish retries only the upload), cancelled episodes get their note + the promised Retry button, full dates + render duration on the lifecycle line, review/published flashes; index.html — triage toasts say what the server actually did (re-render vs upload vs review) |quote selector + signature field (ADR-056, picking Quote auto-tunes) on the New Campaign form, a separate "Image model" card on Credentials (provider-chain input with Gemini-first / Pollinations-first / Kontext-uses-your-image / Pollinations-only preset buttons — ADR-053/055) + an optional Pollinations token field with a Test button; `operations.html` (ADR-058) — the System-group factory floor: URL-driven ⏳ Render queue / ⚙ Worker tabs, a worker verdict banner shown on every tab (down / wedged), queue rows with 🔼 Next + ✕ Cancel, a worker card with the live render's progress + how long since it last moved, a Recovery card (🩹 Recover stuck renders · 🔄 Restart worker) the app bar (`base.html`) is now present at EVERY width — brand + the dynamic breadcrumb trail + alert bell + theme + signed-in address — with the sidebar sticking below it and dropping its duplicate brand/email outside the phone drawer. Every page's breadcrumb moved out of the content flow into a `{% block crumbs %}` the app bar renders (ADR-061), with `_campaign_crumbs.html` holding the campaign-hub trail so the three hub pages share one definition (a block cannot be filled from inside an include); on the phone the brand yields to the trail (`:has()`), which scrolls rather than truncating each crumb to a letter; a per-channel growth block on each Channels card (subs/views/published totals with deltas + a hand-rolled dual-series chart: CSS bars for episodes published per day, an inline-SVG polyline for subscribers gained per day, all widths from numeric server values; a single sample says "the curve appears tomorrow" instead of drawing a flat line); ONE dashboard "Factory" card (ADR-066 — published 7d + all-time · views with the measured-episode caveat · retention with its window named · worst-case runway · next publish linking the calendar · renders today), replacing two adjacent same-layout cards that split one question; CPU/RAM moved into the health strip where the other infra signals live; the six stat tiles are gone (every number already had a home); the green all-clear can no longer sit beside a red degraded banner; the activity feed collapses runs of identical events; the sidebar scope switcher is visibly disabled on the whole-factory Dashboard instead of silently filtering nothing; `calendar.html` is the one "what publishes when" surface (ADR-067): week grid with ✏ own-time chips + a List & actions view carrying ⚡ Now (now confirm-gated) and the ✏ Reschedule disclosure, and a plain-language "how a frozen render heals itself" explainer of the three recovery layers; first-hour survival (ADR-068): `index.html` opens with the setup checklist (and holds the only copy of those three steps — the activity card's duplicate is gone) while `All clear` waits for setup, `channels.html` explains a missing Google OAuth client and quotes Facebook's own refusal instead of silently saving a dead Page, `credentials.html` links each free key to the page that issues it, says what breaks without it and folds the two model-chain cards away (they are expert tuning, and the Test button used to overwrite the Pollinations/Freesound explanation), `campaign_new.html` reports AI failures inline with a link to Credentials rather than in a lost `alert()` and explains why a first campaign starts in Review mode, `episode.html` names a failure's cause + fix and links to it with the raw text folded underneath, every `data-confirm` carries a `data-confirm-verb` and names the campaign or episode, and `error.html` is a styled 404/403/500 that keeps the whole rail so there is a way back; a refused Add-a-Page re-opens the disclosure prefilled, OPENS the token guide its own message points at, and says “Why:” rather than “Facebook said:” — the reason is sometimes Graph's sentence and sometimes our translation of it (ADR-077); it repeats the error AT the form — it is the last element of a multi-screen page, so a top-of-page banner was invisible to whoever had just submitted from the bottom (ADR-074); the Channels page explains how to obtain a permanent Page token (shared `_fb_token_help.html`, so Add-a-Page and Replace-token cannot drift), tells an expired PAGE to paste a new token rather than "reconnect via OAuth", and carries a per-Page token-replacement panel (ADR-072); the campaign form gained a Voice-delivery selector (honest about not being a real whisper), 🌙 marks on the curated soft voices, a grade dropdown driven by the shared catalog, the retro-anime 80s/90s preset, and a Quote auto-tune that sets all six genre settings — only where unset — then TOASTS what it changed, because most of those fields live on another tab inside a collapsed section (ADR-071); the Restart-worker confirm describes what actually happens now that boot recovery exists (ADR-070); `settings.html` gains the Rendering card (image-vendor timeout knob with the resume story in its hint — ADR-069); the Channels card says when a channel publishes fine but cannot be MEASURED (a permanent analytics 403) and offers the reconnect, the growth caption reports the calendar span AND the sample count because an offline day leaves a hole, and the campaign hub says plainly that Facebook reports no retention rather than showing “No data” forever (ADR-076); every secret box on every page is rendered by ONE `ui.secret()` macro that tells five password managers not to manage it, and the Facebook token box says at the field — before submit, in the operator's eye-line — when its value is a Page ID, all digits, or too short to be a token (ADR-075); review surfaces render QC's THIRD state — ⚪ "Auto-QC could not run" with the classified reason, a warning when an earlier render failed QC, and a "Run QC now" button; the autopilot feed renders council proposals with confidence + cited facts (plus 🚧 refused / 💰 milestone / 🧠 council icons) and each channel row on Autopilot + Channels carries the council's one-line manager's note; the episode page replays the render's `journey` timeline (ADR-084); the campaign hub Overview decides autopilot proposals in place, the calendar marks pending slot-change proposals, and the Autopilot strip carries the per-channel "Run council now" button + honest outcome banners (ADR-086); compilation cards show their deterministic-only sanity verdict (ADR-087); the dashboard triage failed-row renders the diagnosis's action link (Review settings / Fix the channel / …) beside Retry, same as the episode page — Retry must never be the only clickable thing when the classified fix lives elsewhere (ADR-068, R21); ADR-090: the Approve button names what it does ("publish at the next slot" when one exists), campaign cards show gate AND schedule instead of only "Review-first", and the calendar draws unapproved renders as `pending` cells on the slots they would take | context | HTML | jinja2 | DONE | 2026-08-06 |
| `static/` | web | `app.css` design system (dark-first + optional light theme via [data-theme]; adaptive-first: fluid clamp() type + gutter, intrinsic auto-fit grids, container-query table stacking, centred fluid main column; tokens colour/type/spacing/radii/elevation; one semantic status-colour set reused by pills/banners/charts; 12-col grid; three responsive shell tiers: full sidebar >1024 · icon rail 721–1024 · off-canvas drawer + bottom tab bar ≤720; skeletons; hand-rolled CSS/SVG data-viz bars + sparkline; modal/toast/badge/chip components) + `ui.js` shared helpers (busy-button, drawer + theme toggle, /api/summary poller → attention badge + dashboard auto-refresh + "Live" pulse + reconnect toast, confirm dialog w/ type-to-confirm, toasts, relative-time, channel scope switcher, ⌘K global-search palette (keyboard-driven, XSS-safe DOM build); both pollers are visibility-aware — pause when the tab is backgrounded, resume + refresh on return) + `app.js`, now just the live-row updater for the one episode table (ADR-065): polls `/api/tasks?live=1`, moves each working row's stage pill + progress with textContent/class changes only, drops a row's hook once it settles, 401→/login, pauses when backgrounded — the whole second-table driver (its own search grammar, pager and status words) went away with `/tasks` — no runtime CDN; `.cast-list` styling for the Studio-Mode character manager; `ui.js` also badges the Operations rail item when the worker is down OR wedged and words the degraded note for a registered-but-wedged worker (ADR-057/058); grid utilities completed to `.col-4/6/8` (+ mobile collapse) with `.mt-md`/`.text-bad`/`.row-actions.center`; `.growth`/`.gc-bars`/`.gc-line` for the channel growth chart; `.resched`/`.ops-table` for the per-episode reschedule disclosure (top-aligned rows so an open panel never shifts its neighbours); the app bar is shown at all widths (`.topbar` + `.topbar-right`, sidebar offset to `--topbar-h`, phone-only `.nav-toggle`) and `ui.js` gained the alert bell — a 30s visibility-aware `/api/alerts` poll, a live red/amber count that clears itself (no read state to go stale), and a panel built with createElement/textContent since channel/campaign/error text is user data; the panel is viewport-anchored on the phone, where a bell-anchored 420px dropdown would hang off the left edge; `.topbar-crumbs` truncates on desktop and scrolls on the phone, and `.crumbs` lost its content-flow margins now that the trail lives only in the app bar; ADR-068: `ui.js` gained `initFlash` (a flash describes something that just happened, so `?flash=` is stripped from the address bar via `replaceState` — a reload or a shared link re-showed a success for an action nobody took), `CMD_PAGES` + `fold` (the ⌘K palette lists destinations as well as content (and preselects its first row on open — ⌘K then Enter used to do nothing while ⌘K, a letter, Enter worked, ADR-070), matched locally so they answer instantly and survive a failed request, with the server's diacritic folding mirrored client-side) and a `verb` argument to `confirmDialog` (`data-confirm-verb`) so the button says what it does; `app.css` adds the `.setup` first-run checklist, `.err-page`, `.mono-wrap` (raw error text must wrap or a long provider URL scrolls the phone sideways) a save bar that WRAPS on the phone — one non-wrapping row could not fit two submit buttons, Cancel and the hint at 375px, so flex shrank them and the primary action clipped to "Create &" (desktop keeps its single row); and a ≥44px floor on every phone control — buttons, `.btn.sm` row actions, inputs, tab strips, `.icon-btn` — with filter chips deliberately at 40px; R22: app.js live rows fetch an absent episode's terminal state once instead of freezing at the last working frame; `.slot.pending` marks a calendar slot whose video is rendered but unapproved — neither filled nor missed (ADR-090) | — | CSS/JS | — | DONE | 2026-08-06 |

## Ops / infra

| Path | Layer | Responsibility | Inputs | Outputs | Depends on | Status | Last updated |
|------|-------|----------------|--------|---------|------------|--------|--------------|
| `docker-compose.yml` | infra | 4 services: redis, web (expose-only), worker (1 cpu-capped, progress-aware healthcheck via `worker_healthy` — ADR-057), cloudflared; web/worker run the GHCR image `ghcr.io/<owner>/<repo>:${AVF_IMAGE_TAG:-latest}` | `.env` | running stack | docker | DONE | 2026-07-28 |
| `Dockerfile` | infra | Shared image for web+worker; installs ffmpeg (apt), Python deps | requirements.txt | image | docker | DONE | 2026-07-17 |
| `requirements.txt` | infra | Pinned runtime Python dependencies (ARM-wheel aware); edge-tts kept CURRENT (stale versions 403 when Microsoft rotates the endpoint auth) | — | deps | pip | DONE | 2026-07-21 |
| `requirements-dev.txt` | infra | Test/lint deps (pytest, fakeredis, ruff); not in runtime image | — | deps | pip | DONE | 2026-07-17 |
| `Dockerfile`/`.env.example`/`.gitignore` | infra | Shared image build; env template; ignore rules | — | — | docker | DONE | 2026-07-17 |
| `config/tunnel_config.yml` | infra | cloudflared ingress mapping to `web:8000` (documentation of the tunnel) | `TUNNEL_TOKEN` | tunnel routes | cloudflared | DONE | 2026-07-17 |
| `scripts/backup_db.sh` | ops | WAL checkpoint → `VACUUM INTO` → `.dump` plaintext SQL → push to backup repo | `factory.db`, `GITHUB_PAT` | `factory_dump.sql` | sqlite3, git | DONE | 2026-07-19 |
| `scripts/check_docs.py` | ops | Fail if a source file has no `SYSTEM_MAP.md` row (docs-drift guard) | tree, this file | pass/fail | stdlib | DONE | 2026-07-17 |
| `.github/workflows/backup.yml` | ops | Verify committed dump restores + integrity check + retention prune | backup repo | CI result | GitHub Actions | DONE | 2026-07-17 |
| `.github/workflows/test.yml` | ops | CI: install ffmpeg + deps, ruff lint, run pytest (no secrets) | push/PR | CI result | GitHub Actions | DONE | 2026-07-17 |
| `.github/workflows/deploy.yml` | ops | CD (ADR-015): build linux/arm64 image → push to GHCR → SSH ship compose+deploy.sh, GHCR login (ephemeral token), pull + restart | merge to main | pushed image + deployed stack | GitHub Actions, buildx/QEMU, GHCR, ssh | DONE | 2026-07-20 |
| `scripts/deploy.sh` | ops | On-VPS deploy: docker compose pull (pinned GHCR tag) + up -d, health-gate, image prune (keeps .env/volumes) | shipped compose, `.env`, AVF_IMAGE_TAG | running stack | docker compose | DONE | 2026-07-20 |
| `tests/` | test | pytest suite (crypto, isolation, ai/safety, render units, worker, scheduler, web, services, ffmpeg integration, QC three-state routing, approval-vs-schedule separation) | — | test results | pytest, fakeredis | DONE | 2026-08-06 |
| `pytest.ini` | test | pytest config (testpaths=tests) | — | — | pytest | DONE | 2026-07-17 |

## Docs

| Path | Responsibility | Status | Last updated |
|------|----------------|--------|--------------|
| `CLAUDE.md` | Agent contract, hard constraints, Definition of Done | DONE | 2026-07-17 |
| `docs/CODING_STANDARDS.md` | DRY/KISS/YAGNI/SOLID/Boy-Scout with repo-concrete examples | DONE | 2026-07-17 |
| `docs/SYSTEM_MAP.md` | This living module map | DONE | 2026-07-17 |
| `docs/ROADMAP.md` | Phased task list with status tokens | DONE | 2026-07-17 |
| `docs/ARCHITECTURE.md` | Topology, trust boundaries, ADR log | DONE | 2026-07-17 |
| `docs/RUNBOOK.md` | Ops procedures (restore, rotate, recover, redeploy) | DONE | 2026-07-17 |
| `README.md` | Human onramp + quick start | DONE | 2026-07-17 |
